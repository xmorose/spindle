PORT=3590
# Shared secret the plugin must send in the X-Spindle-Secret header
INGEST_SECRET=change-me
# Path to Spindle's own event store (created if missing)
STATS_DB_PATH=./data/stats.db
# Read-only path to Navidrome's database
NAVIDROME_DB_PATH=/data/navidrome.db
# Session gap threshold in minutes
SESSION_GAP_MINUTES=30
